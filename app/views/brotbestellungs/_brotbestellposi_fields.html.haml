.nested-fields
  .field
    %label
    .three.fields
      .field
        = f.select :brotsorte_id, options_from_collection_for_select(Brotsorte.all, :id, :name, f.object.brotsorte_id), {}, {class: 'ui dropdown'}
      .field
        = f.number_field :menge
      .field
        = link_to_remove_association '<i class="fa fa-times-circle" aria-hidden="true"></i>'.html_safe, f, {class: 'ui red button'}
